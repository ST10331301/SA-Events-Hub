<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SAEventHub" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryVariant">@color/sa_red</item>
        <item name="colorOnSecondary">@color/on_accent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="cardViewStyle">@style/CardViewStyle</item>
    </style>

    <style name="Theme.SAEventHub" parent="Base.Theme.SAEventHub" />

    <!-- For the splash screen -->
    <style name="Theme.SAEventHub.Startup" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/bg_gradient</item>
    </style>

    <style name="CardViewStyle" parent="CardView">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">4dp</item>
        <item name="contentPadding">8dp</item>
    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.Material3.Dark.ActionBar">
        <item name="colorPrimary">@color/primary</item>
    </style>

    <style name="PopupOverlay" parent="ThemeOverlay.Material3.Light">
        <item name="colorPrimary">@color/primary</item>
    </style>

    <!-- Button Styles -->
    <style name="SAButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/primary</item>
        <item name="android:textColor">@color/on_primary</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <style name="SAButtonOutlined" parent="Widget.Material3.Button.OutlinedButton">
        <item name="strokeColor">@color/primary</item>
        <item name="android:textColor">@color/primary</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- Chip Styles -->
    <style name="ChipStyle" parent="Widget.Material3.Chip.Assist">
        <item name="chipBackgroundColor">@color/surface</item>
        <item name="chipStrokeColor">@color/primary</item>
        <item name="chipStrokeWidth">1dp</item>
        <item name="android:textColor">@color/primary</item>
        <item name="chipCornerRadius">16dp</item>
        <item name="chipMinTouchTargetSize">0dp</item>
    </style>

</resources>